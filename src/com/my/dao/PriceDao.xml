<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.my.dao.PriceDao">
    
    <select id="findPriceById" parameterType="Integer"
        resultType="Price">
        
        select * from tb_exprice where id=#{id}
        </select>
    
</mapper>